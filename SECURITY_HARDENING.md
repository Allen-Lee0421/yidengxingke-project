# 網站防毒／防駭加固說明

本次已將網站伺服器的防護基線提升，重點包括安全標頭、同源優先的 CORS、請求大小限制、一般與敏感 API 限流、資料庫連線逾時、WAF 告警寫入錯誤處理、伺服器資訊隱藏，以及將特權碼由程式碼移至環境變數。會員 session 不再接受預設密鑰；生產環境若未提供至少 32 字元的 `SESSION_SECRET`，登入會失敗而不會以弱密鑰運作。

## 上線前必要操作

1. 立即在代理後台變更附件中曾出現的密碼，並撤銷或輪替曾暴露的 API keys。這些值不應放在網站前端、Git、Issue、日誌或 `.env.example`。
2. 於正式部署平台設定 `SESSION_SECRET`、`DATABASE_URL`、`ADMIN_EXPORT_TOKEN`、`ALLOWED_ORIGINS` 與 `PRIVILEGE_CODES_JSON`。`PRIVILEGE_CODES_JSON` 應使用新的隨機代碼，不得沿用舊代碼。
3. 設定 `TRUST_PROXY=true` 僅限網站確實位於一層可信任反向代理後方；否則維持 `false`，避免 IP 限流被偽造標頭繞過。
4. 啟用 HTTPS，並確認 HSTS 僅在所有正式子網域均支援 HTTPS 後使用。資料庫應使用 TLS；只有本機開發才可將 `DATABASE_SSL=false`。
5. 建立資料庫帳號最小權限，限制資料庫只接受應用程式主機連線；將備份加密並定期測試還原。
6. 在部署平台與 CDN/WAF 啟用惡意流量規則、Bot 管理、登入端點額外限流、漏洞告警與日誌保留。應用層 WAF 只是第二道防線，不取代專業 CDN/WAF 與主機防毒。
7. 上線前以新的秘密掃描、依賴稽核、TLS 掃描與未授權 API 測試驗證；不要把個資、IP 原文或敏感 payload 長期寫入無存取控制的日誌。

## 明確限制

網站端無法替伺服器安裝或取代主機級防毒軟體。若使用 VPS／Docker，仍應在主機或雲端工作負載層啟用 EDR／惡意檔案掃描、唯讀容器檔案系統、非 root 使用者、映像檔簽章與自動安全更新。

## 已知待處理風險

專案仍包含其他歷史 PHP／Django 子目錄，應分別確認是否部署；若未使用，建議移出正式部署內容。若要啟用付款、LINE webhook 或代理後台整合，必須另外加入正式供應商簽章驗證、重放攻擊防護、權限分離與個資／法遵審查，不應直接以目前的示範端點承接真實交易。
